#include<stdio.h>
int main()
{

    int a,max,min;
    printf("(输入-1结束)输入整数:");
    scanf("%d",&a);
    max=a;min=a;
    while(a>=0)
    {
        if(max<a)
            max=a;
		if(min>a)
			min=a;
        scanf("%d",&a);
    }
    printf("最大值是%d,最小值是%d\n",max,min);
    return 0;
}
